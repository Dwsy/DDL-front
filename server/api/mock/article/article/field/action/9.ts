import { defineEventHandler } from 'h3'

export default defineEventHandler((event) => {
  return action
})
const action = {
  code: 0,
  msg: '',
  data: { thumb: 2, watch: false, collect: true, follow: false },
}
